<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  {% include "shared/_menu.jinja" %}
  {% include "shared/_flashes.jinja" %}

  <h1>Viagens</h1>
  {% if posts %}
    {% for post in posts %}
      <a href="{{ url_for('posts.show', id=post.id) }}">{{ post.title }}</a>
    {% endfor %} 
  {% else %}
  <p>Sem posts cadastrado no momento</p>
  {% endif %}

  <p>&COPY; Todos os direitos reservados. Be Travel 2022</p>
</body>
</html>